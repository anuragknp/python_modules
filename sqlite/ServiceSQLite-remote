#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import ServiceSQLite
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  ResultSetArrayAndTimeTaken ExecuteStatementArray(DirLevel dir_level, string db_name, StatementArray statement_array, i64 user_id, bool wrap_inside_transaction, i64 sqlite_flags, string app_name, i64 busy_timeout)'
  print '  void ping()'
  print '  void InitUserDirectories(i64 user_id)'
  print '  UserMaintenanceConf GetMaintenance(i64 user_id)'
  print '  void SetMaintenance(i64 user_id, UserMaintenanceConf conf)'
  print '  void RemoveMaintenance(i64 user_id)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = ServiceSQLite.Client(protocol)
transport.open()

if cmd == 'ExecuteStatementArray':
  if len(args) != 8:
    print 'ExecuteStatementArray requires 8 args'
    sys.exit(1)
  pp.pprint(client.ExecuteStatementArray(eval(args[0]),args[1],eval(args[2]),eval(args[3]),eval(args[4]),eval(args[5]),args[6],eval(args[7]),))

elif cmd == 'ping':
  if len(args) != 0:
    print 'ping requires 0 args'
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'InitUserDirectories':
  if len(args) != 1:
    print 'InitUserDirectories requires 1 args'
    sys.exit(1)
  pp.pprint(client.InitUserDirectories(eval(args[0]),))

elif cmd == 'GetMaintenance':
  if len(args) != 1:
    print 'GetMaintenance requires 1 args'
    sys.exit(1)
  pp.pprint(client.GetMaintenance(eval(args[0]),))

elif cmd == 'SetMaintenance':
  if len(args) != 2:
    print 'SetMaintenance requires 2 args'
    sys.exit(1)
  pp.pprint(client.SetMaintenance(eval(args[0]),eval(args[1]),))

elif cmd == 'RemoveMaintenance':
  if len(args) != 1:
    print 'RemoveMaintenance requires 1 args'
    sys.exit(1)
  pp.pprint(client.RemoveMaintenance(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
